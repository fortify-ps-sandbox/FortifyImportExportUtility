logging:
  level:
    org.springframework.beans.factory: WARN
    com.fortify.impexp.common.api.processor.AbstractEnabledProcessorsLoader: TRACE

spring:
  profiles:
    active: default
  main:
    web-application-type: NONE

from.ssc:
  conn:
    baseUrl: http://localhost:1920/ssc
    userName: ssc
    password: Fortify123!
  
from.mock:
#  enabled: true

to.mock:
  simple: test
  entity:
    sources: mock, ssc
    types: vulnerability, mock_custom